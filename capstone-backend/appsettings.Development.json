{
  "Logging": {
    "LogLevel": {
      "Default": "Debug",
      "Microsoft.AspNetCore": "Information",
      "Microsoft.EntityFrameworkCore.Database.Command": "Information",
      "Microsoft.Extensions.Http": "Warning"
    }
  },
  
  "ConnectionStrings": {
    "DefaultConnection": "Host=${DB_HOST:localhost};Port=${DB_PORT:5432};Database=${DB_NAME:capstone_db_dev};Username=${DB_USER:postgres};Password=${DB_PASSWORD:postgres}"
  }
}
