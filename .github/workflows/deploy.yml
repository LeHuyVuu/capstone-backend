name: Deploy to VPS (Blue-Green)

on:
  push:
    branches: [ main, master ]
  workflow_dispatch:

env:
  DOCKER_IMAGE: lehuyvuuu/capstone-backend
  APP_NAME: capstone-backend
  BLUE_PORT: 5224
  GREEN_PORT: 5225
  CONTAINER_PORT: 5224
  ENV_DIR: /root/capstone
  BACKEND_FILE: /etc/nginx/snippets/capstone-upstream.conf
  HEALTH_PATH: /api/health

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_TOKEN }}

      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: |
            ${{ env.DOCKER_IMAGE }}:latest
            ${{ env.DOCKER_IMAGE }}:${{ github.sha }}
          cache-from: type=registry,ref=${{ env.DOCKER_IMAGE }}:buildcache
          cache-to: type=registry,ref=${{ env.DOCKER_IMAGE }}:buildcache,mode=max

      - name: Deploy to VPS (Blue-Green)
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          password: ${{ secrets.VPS_PASSWORD }}
          port: 22
          script: |
            set -euo pipefail

            echo "🔐 Docker login"
            echo "${{ secrets.DOCKER_TOKEN }}" | docker login -u "${{ secrets.DOCKER_USERNAME }}" --password-stdin

            echo "🧾 Write env file"
            sudo mkdir -p ${{ env.ENV_DIR }}
            sudo tee ${{ env.ENV_DIR }}/.env > /dev/null <<'EOF'
            ${{ secrets.ENV_FILE }}
            EOF

            echo "📥 Pull image"
            docker pull ${{ env.DOCKER_IMAGE }}:latest

            echo "🔎 Detect current backend port"
            ACTIVE_PORT=""
            if sudo test -f "${{ env.BACKEND_FILE }}"; then
              ACTIVE_PORT=$(sudo grep -Eo '127\.0\.0\.1:[0-9]+' "${{ env.BACKEND_FILE }}" | cut -d: -f2 || true)
            fi

            if [[ "$ACTIVE_PORT" == "${{ env.BLUE_PORT }}" ]]; then
              NEW_COLOR="green"
              OLD_COLOR="blue"
              NEW_PORT="${{ env.GREEN_PORT }}"
              OLD_PORT="${{ env.BLUE_PORT }}"
            else
              NEW_COLOR="blue"
              OLD_COLOR="green"
              NEW_PORT="${{ env.BLUE_PORT }}"
              OLD_PORT="${{ env.GREEN_PORT }}"
            fi

            echo "🔵🟢 Deploy $NEW_COLOR on port $NEW_PORT (old $OLD_COLOR on $OLD_PORT)"

            echo "🧹 Cleanup same-color container"
            docker stop ${{ env.APP_NAME }}-$NEW_COLOR 2>/dev/null || true
            docker rm   ${{ env.APP_NAME }}-$NEW_COLOR 2>/dev/null || true

            echo "🚀 Run new container"
            docker run -d \
              --name ${{ env.APP_NAME }}-$NEW_COLOR \
              --restart unless-stopped \
              -p 127.0.0.1:$NEW_PORT:${{ env.CONTAINER_PORT }} \
              -e ASPNETCORE_URLS=http://+:${{ env.CONTAINER_PORT }} \
              --env-file ${{ env.ENV_DIR }}/.env \
              ${{ env.DOCKER_IMAGE }}:latest

            echo "⏳ Health check"
            for i in {1..30}; do
              if curl -fsS "http://127.0.0.1:$NEW_PORT${{ env.HEALTH_PATH }}" >/dev/null; then
                echo "✅ Healthy"
                break
              fi
              echo "Attempt $i/30..."
              sleep 2
            done

            if ! curl -fsS "http://127.0.0.1:$NEW_PORT${{ env.HEALTH_PATH }}" >/dev/null; then
              echo "❌ Healthcheck failed, rollback"
              docker logs --tail 200 ${{ env.APP_NAME }}-$NEW_COLOR || true
              docker stop ${{ env.APP_NAME }}-$NEW_COLOR || true
              docker rm   ${{ env.APP_NAME }}-$NEW_COLOR || true
              exit 1
            fi

            echo "🔁 Switch Nginx backend (atomic)"
            sudo mkdir -p /etc/nginx/snippets
            sudo tee "${{ env.BACKEND_FILE }}.tmp" > /dev/null <<EOF
            set \$backend http://127.0.0.1:$NEW_PORT;
            EOF
            sudo mv "${{ env.BACKEND_FILE }}.tmp" "${{ env.BACKEND_FILE }}"

            sudo nginx -t
            sudo systemctl reload nginx
            echo "✅ Nginx now points to $NEW_PORT"

            echo "🛑 Stop old container"
            if docker ps -q --filter "name=^${{ env.APP_NAME }}-$OLD_COLOR$" | grep -q .; then
              docker stop ${{ env.APP_NAME }}-$OLD_COLOR || true
              docker rm   ${{ env.APP_NAME }}-$OLD_COLOR || true
            fi

            echo "🧽 Cleanup old images"
            docker image prune -af --filter "until=48h" || true

            echo "🎉 Deploy completed. Active: $NEW_COLOR ($NEW_PORT)"
